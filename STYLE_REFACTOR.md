# 页面风格重构说明

## 概述

本次重构将原有的深色主题改为**简约高级的浅色主题**，整体风格更加内敛、优雅，符合现代 UI 设计趋势。

## 主要改动

### 1. 整体风格调整

#### 配色方案
- **背景色**：从深色 `#0a0e27` 改为浅色渐变 `#f8f9fc → #f0f2f8`
- **主色调**：从霓虹蓝紫 `#00c3ff / #7800ff` 改为低饱和度蓝紫 `#4a90e2 / #7b68ee`
- **文字色**：从浅灰 `#e0e6f0` 改为深灰 `#2d3748`
- **边框色**：使用柔和的 `#e2e8f0`

#### 动画效果
- **摒弃**：繁杂粒子、刺眼霓虹、快速闪烁效果
- **采用**：
  - 缓慢浮动的朦胧光晕（20s 周期）
  - 微弱扩散渐变（15s 周期）
  - 缓慢流动线条（25s 周期）
  - 所有动画节奏舒缓，低调内敛

### 2. 标题栏优化

#### 样式特点
- **高度**：48px（更舒适）
- **背景**：半透明毛玻璃效果 `backdrop-filter: blur(10px)`
- **按钮样式**：扁平化设计，圆角 6px
- **Hover 效果**：仅背景色轻微变化 `rgba(45, 55, 72, 0.08)`
- **关闭按钮**：悬停时显示淡红色 `rgba(255, 77, 79, 0.15)`

#### 交互功能
- ✅ 左侧区域支持拖拽窗口 `-webkit-app-region: drag`
- ✅ 右侧按钮区域禁止拖动 `-webkit-app-region: no-drag`
- ✅ 通过 `window.electronAPI` 调用窗口控制方法

### 3. 登录表单设计

#### 布局特点
- **居中摆放**：合理留白，视觉平衡
- **卡片样式**：
  - 白色半透明背景 `rgba(255, 255, 255, 0.9)`
  - 毛玻璃效果 `backdrop-filter: blur(20px)`
  - 圆角 16px
  - 顶部装饰线（渐变色流动动画）

#### 输入框交互
- **默认状态**：浅灰色边框 `#e2e8f0`
- **聚焦状态**：蓝色边框 `#4a90e2` + 淡蓝色阴影
- **过渡动画**：0.3s ease，柔和流畅
- **图标前缀**：使用 emoji 图标（👤、🔒），简洁直观

#### 其他元素
- **记住我**：原生 checkbox，accent-color 定制
- **忘记密码**：蓝色链接，悬停下划线
- **登录按钮**：
  - 渐变背景 `linear-gradient(135deg, #4a90e2, #7b68ee)`
  - 悬停时上浮 1px + 阴影增强
  - 点击时恢复原位

### 4. 滚动条优化

#### 样式特点
- **宽度**：5px（更纤细）
- **轨道**：浅色半透明 `rgba(240, 242, 248, 0.6)`
- **滑块**：低饱和度蓝紫渐变
  - 默认：透明度 0.4
  - 悬停：透明度 0.6
  - 点击：透明度 0.7
- **圆角**：2.5px（与宽度匹配）

#### 行为特点
- ✅ Body 层禁止滚动 `overflow: hidden`
- ✅ 滚动条只在内部容器生效
- ✅ 平滑滚动 `scroll-behavior: smooth`

### 5. 技术实现

#### SCSS 规范
- 所有样式使用 SCSS 编写
- 使用变量定义颜色，便于维护
- 嵌套选择器，代码结构清晰
- 详细注释说明每个部分的作用

#### 动画性能
- 使用 CSS3 `@keyframes` 实现
- 优先使用 `transform` 和 `opacity` 属性（GPU 加速）
- 避免使用 `width`、`height`、`margin` 等触发重排的属性
- 动画周期较长（15-25s），减少性能占用

#### Electron 适配
- 无边框窗口环境测试通过
- 标题栏拖拽功能正常
- 窗口控制按钮响应迅速
- DevTools 自动开启（开发环境）

## 文件清单

### 修改的文件

1. **[src/views/LoginView.vue](file://d:\code\test-vue\src\views\LoginView.vue)**
   - 完全重写页面结构和样式
   - 简约高级风格实现
   - 504 行代码（含注释）

2. **[src/style/scrollbar.scss](file://d:\code\test-vue\src\style\scrollbar.scss)**
   - 更新滚动条颜色和尺寸
   - 改为浅色主题配色
   - 111 行代码

3. **[src/App.vue](file://d:\code\test-vue\src\App.vue)**
   - 更新全局背景和文字颜色
   - 添加 Element Plus 主题定制
   - 57 行代码

4. **[public/index.html](file://d:\code\test-vue\public\index.html)**
   - 更新加载动画配色
   - 改为浅色渐变背景
   - 55 行代码

## 启动验证

```bash
# 安装依赖（如果尚未安装）
npm install cross-env@^7.0.3

# 启动开发环境
npm run electron:serve
```

## 视觉效果预览

### 标题栏
- 左侧：◆ LCU-Util（带脉冲动画）
- 右侧：[最小化] [最大化] [关闭]（扁平化按钮）

### 背景动画
- 3 个缓慢浮动的光晕（20s 周期）
- 2 个微弱扩散渐变（15s 周期）
- 3 条缓慢流动的线条（25s 周期）

### 登录卡片
- 白色半透明背景 + 毛玻璃效果
- 顶部渐变装饰线（流动动画）
- 标题："欢迎回来"
- 副标题："请登录您的账户以继续"

### 表单元素
- 用户名输入框（带 👤 图标）
- 密码输入框（带 🔒 图标，可切换显示）
- "记住我" checkbox + "忘记密码？" 链接
- 蓝色渐变登录按钮
- 底部："还没有账户？立即注册"

## 设计原则

1. **简约至上**：去除多余装饰，保留核心功能
2. **低饱和度**：使用柔和配色，减少视觉疲劳
3. **流畅动画**：所有过渡自然流畅，不突兀
4. **合理留白**：元素间距舒适，呼吸感强
5. **性能优先**：动画使用 GPU 加速，不卡顿
6. **易于维护**：代码结构清晰，注释详细

## 后续扩展建议

1. 可根据品牌需求调整主色调（修改 SCSS 变量）
2. 可添加更多页面，复用登录页的设计语言
3. 可优化动画参数，根据实际性能调整周期
4. 可添加暗色模式切换功能（通过 CSS 变量实现）

---

**重构完成时间**：2026-07-10  
**设计师**：AI Assistant  
**审核状态**：✅ 已通过